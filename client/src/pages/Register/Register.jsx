import React from 'react';

/**
 * Register Page Component
 * 
 * Purpose: Allows new users to create accounts.
 * 
 * TODO:
 * - Implement state bindings for user inputs.
 * - Call authService to register new credentials on submit.
 */
function Register() {
  return (
    <div>
      <h1>Register</h1>
      <p>TODO: Add name, email, password inputs and validation logic.</p>
    </div>
  );
}

export default Register;
