const express = require('express');
const router = express.Router();
const analyticsController = require('../controllers/analyticsController');

// TODO: Bind GET /:id (get URL analytics metrics, auth required)

module.exports = router;
